export class Vector {
    /**
     * Construct a vector
     * @param {number} x The X coordinate
     * @param {number} y The Y coordinate
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}